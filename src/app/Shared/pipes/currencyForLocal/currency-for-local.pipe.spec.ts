import { CurrencyForLocalPipe } from './currency-for-local.pipe';

describe('CurrencyForLocalPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyForLocalPipe();
    expect(pipe).toBeTruthy();
  });
});

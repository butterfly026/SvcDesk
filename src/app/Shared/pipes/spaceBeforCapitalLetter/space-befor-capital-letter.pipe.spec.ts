import { SpaceBeforCapitalLetterPipe } from './space-befor-capital-letter.pipe';

describe('SpaceBeforCapitalLetterPipe', () => {
  it('create an instance', () => {
    const pipe = new SpaceBeforCapitalLetterPipe();
    expect(pipe).toBeTruthy();
  });
});

import {InputDisableControlDirective} from './input-disable-control.directive';

describe('InputDisableControlDirective', () => {
    it('should create an instance', () => {
        const directive = new InputDisableControlDirective();
        expect(directive).toBeTruthy();
    });
});
